# This config is a STUB! This should never be generated.
# Use the default lua config from https://github.com/hyprwm/Hyprland/blob/main/example/hyprland.lua

$browser = brave
$mainMod = SUPER
$terminal = kitty
$fileManager = dolphin
$menu = rofi -show drun

bind = $mainMod, Q, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, $menu
bind = $mainMod, W, exec, $browser
bind = $mainMod, S, exec, steam
bind = $mainMod, F, fullscreen
bind = $mainMod, G, exec, waypaper
bind = $mainMod, SPACE, exec, qs -c baios ipc call baihub toggle

# Cambiar de escritorio con SUPER + [1-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9

# Mover ventana activa a un escritorio con SUPER + SHIFT + [1-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9

monitor=HDMI-A-1, 1920x1080, 1920x0, 1
monitor=DP-1, 1920x1080, 0x0, 1
# Forzar escritorios a pantallas específicas
workspace = 1, monitor:HDMI-A-1, default:true
workspace = 3, monitor:HDMI-A-1
workspace = 5, monitor:HDMI-A-1

workspace = 2, monitor:DP-1, default:true
workspace = 4, monitor:DP-1
workspace = 6, monitor:DP-1

# exec-once = waybar &
# Inicializa el motor de fondos de pantalla
exec-once = swww-daemon

# Restaura el último fondo de pantalla seleccionado en Waypaper
exec-once = waypaper --restore

exec-once = qs -c baios